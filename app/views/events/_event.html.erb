<li> <%= event.id %> : <%= event.date %>
  <% unless event.attendees.include? current_user %>
    <%= link_to "Attend the event", event_attend_path(event), method: :post %>
  <% end %>
  <%= link_to "Show", event %>
</li>